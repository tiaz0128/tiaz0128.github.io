# 요약본 카탈로그. 문서 자체는 resources/<slug>.html 에 있는 통짜 정적
# 파일이고, 이 파일은 그 목록을 만든다.
#
# 새 문서 넣는 순서
#   1. resources/<slug>.html 에 통짜 HTML 을 넣는다 (프런트매터 없이).
#      slug 는 파일 이름 그대로다 — 소문자 하이픈. /link 가 여기서
#      /resources/<slug>.html 과 .pdf 링크를 그대로 만들어 낸다.
#   2. 같은 이름으로 resources/<slug>.pdf 를 둔다. 받기 버튼이 이 파일을
#      그대로 주기 때문에, 없으면 404 다. 뽑는 길은 둘 다 결과가 같다.
#        - 브라우저 인쇄: A4 · 여백 없음 · 배경 그래픽 켬.
#          배경 그래픽을 빼면 표와 카드의 색이 전부 날아간다.
#        - cd script && uv run shot.py <slug>
#          (새로 깐 WSL 이면 chromium 이 기대는 라이브러리를 한 번만:
#           sudo apt-get install -y libnss3 libnspr4 libasound2t64)
#   3. 이 파일 맨 위에 항목을 추가한다.
#      title/summary 는 문서 표지에 박힌 것과 같게.
#      services: 에 처음 쓰는 키가 있으면 shot.py 를 한 번 돌려야 한다.
#      _data/service_icons.yml 을 그게 만드는데, 아이콘 잉크 경계를 재려고
#      브라우저를 쓰기 때문이다. 안 돌려도 에러는 안 난다 — 그 아이콘만
#      조용히 안 그려지고 일반 문서 아이콘으로 떨어진다.
#   4. 배포. master 에 올리면 .github/workflows/jekyll.yml 이 빌드해서
#      GitHub Pages 로 내보낸다. 다른 절차는 없다.
#
#        git add resources/<slug>.html resources/<slug>.pdf \
#                _data/resources.yml _data/service_icons.yml
#        git commit && git push origin master
#
#      문서를 갈아치웠다면 옛 slug 의 html·pdf 를 git rm 한다. 프런트매터가
#      없어 Jekyll 이 그냥 복사하는 파일이라, 항목만 지우면 목록에서만
#      빠지고 옛 URL 은 계속 열린다.
#      배포가 반영이 안 되면 나온 페이지의 generator 메타를 본다.
#      Jekyll v3.10.0 이면 워크플로가 아니라 레거시 Pages 빌드가 돈 것이다.
#
# 표지는 여기 없다:
#   목록 줄은 전부 같은 꼴이고, 표지는 구역 맨 위에 견본 한 장만 걸린다.
#   그 한 장은 _data/links.yml 의 sample: 이 정하고 새 문서를 따라다니지
#   않는다. 그래서 문서를 넣을 때마다 표지를 굽지 않아도 된다.
#   견본을 갈고 싶을 때만 shot.py 로 그 문서를 굽고 sample: 을 바꾼다.
#
# 왜 컬렉션(_resources/)이 아닌가:
#   컬렉션 문서는 Jekyll::Document라서 jekyll-spaceship의 8개 프로세서가
#   전부 달라붙는다(:documents, pre_render/post_render). 문서 하나가 660KB짜리
#   base64 영상 덩어리라 Nokogiri 파싱과 정규식 스캔에서 빌드가 몇 분씩 멈춘다.
#   render_with_liquid: false 로는 Liquid만 꺼지고 spaceship은 못 막는다.
#   프런트매터가 없으면 Jekyll은 StaticFile로 보고 그냥 복사한다 — 처리 0.
#
# date:    마지막으로 손본 날. 두 군데에 쓰인다 — 목록 순서(최신이 위)와
#          줄에 붙는 "2026-08-30 업데이트". 문서 표지 오른쪽 위의 .stamp 와
#          같은 날을 적는다. 그쪽은 내보낸 날이 자동으로 박히니, 문서를 다시
#          내보냈으면 여기도 같이 고칠 것. 안 고치면 목록만 지난 판을 말한다.
# pages:   A4 몇 장인지. 줄에 "A4 12장"으로 붙는다. 문서의 .sheet 개수다.
# kind:    cheatsheet | exam — services 가 비었을 때 쓰는 기본 아이콘이 갈린다.
# services: 이 문서가 다루는 AWS 서비스들. assets/json/icons/aws.json 의 키.
#           줄 왼쪽 아이콘은 이 중 첫 번째를 쓴다 — 대표를 맨 앞에 둘 것.
#           shot.py 가 쓰인 것만 골라 _data/service_icons.yml 로 뽑아둔다
#           (원본이 2.5MB라 통째로 못 읽는다).
#           비워두면 일반 문서 아이콘으로 떨어진다.
#
#           ★ 한 문서 안에서는 같은 계열로 고를 것. aws.json 에는 색 사각형
#             (Architecture)과 선화(Resource) 두 계열이 섞여 있어서, 같이
#             걸면 크기를 아무리 맞춰도 따로 논다. 이름이 리소스를 가리키는
#             쪽(amazon_ec2_instance)이 대개 선화다.
- slug: ec2-ebs-eni
  title: "EC2 · EBS · ENI"
  summary: "서버 한 대는 무엇으로 이루어지고, 무엇이 남고, 어떻게 연결되는가"
  kind: cheatsheet
  services:
    - amazon_ec2_instance
    - amazon_elastic_block_store_volume
    - amazon_vpc_elastic_network_interface
    - amazon_ec2_elastic_ip_address
  date: 2026-08-30
  pages: 12
